# kubectl patch deployment frontend --patch-file frontend-patch.yaml
spec:
  template:
    spec:
      containers:
      - name: nginx
        volumeMounts: [{name: ui, mountPath: /usr/share/nginx/html}]
      volumes: [{name: ui, configMap: {name: ui}}]
