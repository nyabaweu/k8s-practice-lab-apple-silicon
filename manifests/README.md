# Manifests

Ready-to-apply YAML for the larger scenario builds, organized per scenario.
Each file mirrors the guide exactly — the guide explains every field; these save the typing.

Apply from the matching scenario's namespace unless the file says otherwise.
`scenario-9/gpu-llm.yaml` needs a host-IP substitution — instructions are in the file header.
